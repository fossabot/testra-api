language: java
jdk:
  - oraclejdk8

cache:
 directories:
 - $HOME/.gradle/caches/
 - $HOME/.gradle/wrapper/

script:
  - ./gradlew check
#  - ./gradlew integrationTest
  - ./gradlew jacocoTestReport
after_success:
- bash <(curl -s https://codecov.io/bash)
